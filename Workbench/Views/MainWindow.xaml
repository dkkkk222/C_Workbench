<comCtrl:NoTitleBarWindow x:Class="Workbench.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:comCtrl="clr-namespace:Common.Controls;assembly=Workbench.Controls"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        xmlns:calc="clr-namespace:CalcBinding;assembly=CalcBinding"
        xmlns:utils="clr-namespace:Workbench.Utils"
        mc:Ignorable="d"
        WindowStyle="None"
        UseLayoutRounding="True"
        MinWidth="500"
        Topmost="False"
        Style="{StaticResource DefaulClientWindow}"
        Title="MainWindow" Height="786" Width="1200">
    <Window.Resources>
        <Style x:Key="MenuItemIcon" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Window.Resources>
    <comCtrl:NoTitleBarWindow.TitleBarContent>
        <Border Grid.Row="0" x:Name="CustomTitleBar" Style="{DynamicResource TitleBarBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="10,5">
                    <Image Source="/Resource/20240426-165532.ico"/>
                </Border>
                <Border Grid.Column="1">
                    <Menu Style="{DynamicResource MenuStyle}">
                        <MenuItem Header="文件(_F)" SubmenuOpened="MenuItem_SubmenuOpened">
                            <MenuItem x:Name="NewProject" Header="新建" Click="NewProject_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe620;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem x:Name="OpenProject" Header="打开" Click="OpenProject_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe60a;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem x:Name="SaveProject" 
                                      Header="保存" 
                                      IsEnabled="{Binding HasPpec}"
                                      Click="SaveProject_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe60c;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem x:Name="SaveAsProject" 
                                      Header="另存为" 
                                      IsEnabled="{Binding HasPpec}"
                                      Click="SaveAsProject_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe624;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </MenuItem>
                        <MenuItem Header="工程(_P)" SubmenuOpened="MenuItem_SubmenuOpened">
                            <MenuItem Header="{Binding ConnectName}" Command="{Binding ConnectCommand}" IsEnabled="{Binding HasPpec}">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe625;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="调试" IsEnabled="{Binding IsConnected}">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe61b;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="固化" IsEnabled="{Binding IsConnected}">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe60b;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="下发参数" IsEnabled="{Binding IsConnected}">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe62d;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="工程设置" IsEnabled="{Binding HasPpec}">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe61c;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </MenuItem>
                        <MenuItem Header="工具(_T)">
                            <MenuItem Header="固件升级" Click="BootLoader_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe60f;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="行业计算工具" Click="PowerTool_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe60d;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="串口助手" Click="SerialAssistant_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe622;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <!--<MenuItem Header="Node-Red" Click="NodeRed_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe619;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>-->
                        </MenuItem>
                        <MenuItem Header="设置(_S)">
                            <MenuItem Header="全局设置" Click="GlobalSetting_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe617;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </MenuItem>
                        <MenuItem Header="帮助(_H)">
                            <MenuItem Header="用户手册" Click="UserManual_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe623;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="升级更新" Command="{Binding UpgradeCommand}">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe614;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="关于" Click="About_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xe616;" FontFamily="{StaticResource Iconfont}" Style="{StaticResource MenuItemIcon}"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </MenuItem>
                    </Menu>
                </Border>
                <Border Grid.Column="2"></Border>
                <Border Grid.Column="3">
                    <StackPanel Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="Minimum" Click="Minimum_Click" Content="&#xe630;">
                            <Button.Resources>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Width" Value="40"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" >
                                                    <TextBlock x:Name="tbl" Text="{TemplateBinding Content}" Style="{DynamicResource WindowControlIcon}" FontFamily="{StaticResource Iconfont}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#bee6fd"/>
                                                        <Setter TargetName="tbl" Property="TextElement.Foreground" Value="Black"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Content="&#xe65d;" Style="{StaticResource WindowControlButton}" Visibility="{calc:Binding !IsMaximized,FalseToVisibility=Collapsed}" Click="Maximum_Click"/>
                        <Button Content="&#xe692;" Style="{StaticResource WindowControlButton}" Visibility="{calc:Binding IsMaximized,FalseToVisibility=Collapsed}" Click="Maximum_Click"/>
                        <Button Content="&#xe653;" Style="{StaticResource WindowCloseButton}" Click="Close_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </comCtrl:NoTitleBarWindow.TitleBarContent>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border>
            <!--<StackPanel>
                <wv:WebView2 x:Name="webView" Visibility="Collapsed"/>
                <ContentControl prism:RegionManager.RegionName="ContentRegion"/>
            </StackPanel>-->
            <ContentControl prism:RegionManager.RegionName="{x:Static utils:RegionNames.ContentRegion}"/>
        </Border>
        <Border Grid.Row="1" Style="{DynamicResource FooterBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Padding="30,0,0,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{DynamicResource BottomBarText}" Text="武汉森木磊石科技有限公司" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <Border Style="{DynamicResource SeparatorBorder}"/>
                        <TextBlock Style="{DynamicResource BottomBarText}" Text="当前版本：V1.0.0"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Padding="0,0,30,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" FlowDirection="RightToLeft" x:Name="BottomBarPanel" Visibility="Collapsed">
                        <TextBlock Style="{DynamicResource BottomBarText}" Text="未启动"/>
                        <Border Style="{DynamicResource SeparatorBorder}"/>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" x:Name="ChipPanel">
                            <TextBlock x:Name="ChipName" Style="{DynamicResource BottomBarText}" Text="芯片名称"/>
                            <Border Style="{DynamicResource SeparatorBorder}"/>
                        </StackPanel>
                        <TextBlock x:Name="ProjectName" Style="{DynamicResource BottomBarText}" Text="工程名称"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</comCtrl:NoTitleBarWindow>
