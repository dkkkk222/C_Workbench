<UserControl x:Class="Workbench.Views.PassWordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workbench.Views"
              xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource DarkDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <hc:Row Margin="0,0,0,0">
            <hc:Col Span="16" Offset="4">

                <PasswordBox Width="200" hc:InfoElement.TitleWidth="40" hc:InfoElement.TitlePlacement="Left"
                             hc:InfoElement.Title="密码"  Name="passWord1" hc:InfoElement.Placeholder="请输入"
                             hc:InfoElement.ShowClearButton="True"
                              Margin="0,32,0,0" Password="">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox" BasedOn="{StaticResource PasswordBoxExtend}">
                            <Style.Triggers>
                                <!-- 获得焦点时变色 -->
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource BordTitleBackgroundColor}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>
            </hc:Col>
        </hc:Row>
        <Border Grid.Row="1">
            <hc:UniformSpacingPanel Spacing="15" Height="60" HorizontalAlignment="Center">
                <Button Style="{StaticResource ButtonDefault}" Content="取消" Foreground="{StaticResource ButtonBarIconColor}"  Width="80" Height="30" Background="{StaticResource GenCancelButtonBackgroundColor}" Command="{Binding CancelCommand}" hc:IconElement.Geometry="{StaticResource CloseGeometry}"/>
                <Button Style="{StaticResource ButtonPrimary}" Content="确定" Background="{StaticResource ShowPageButtonBackgroundColor}"  Width="80" Height="30" hc:IconElement.Geometry="{StaticResource SaveGeometry}" Click="Button_Click"/>

            </hc:UniformSpacingPanel>
        </Border>
    </Grid>
</UserControl>
