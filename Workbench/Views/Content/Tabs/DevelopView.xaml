<UserControl x:Class="Workbench.Views.Content.Tabs.DevelopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workbench.Views.Content.Tabs"
             xmlns:converters="clr-namespace:Workbench.Utils.ConverterUtil"
             xmlns:utils="clr-namespace:Workbench.Utils"
             xmlns:viewModels="clr-namespace:Workbench.ViewModels.Pages"
             xmlns:views="clr-namespace:Workbench.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:TreeViewItemMarginConverter x:Key="TreeViewItemMarginConverter"/>
        <DataTemplate DataType="{x:Type viewModels:ParamSettingViewModel}">
            <views:ParamSettingView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!--<Border Background="Transparent" Width="225">
            <TreeView x:Name="developTreeView" ItemsSource="{Binding Menus}"
                      SelectedItemChanged="developTreeView_SelectedItemChanged"
                      ItemContainerStyle="{DynamicResource TreeViewItemStyle}"
                      Style="{DynamicResource TreeViewStyle}">
                <TreeView.ItemTemplate >
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal" Height="28" >
                            <TextBlock Text="{Binding Name}" 
                                       FontSize="14" 
                                       VerticalAlignment="Center" 
                                       Foreground="{DynamicResource PrimaryFontColor}"
                                       Margin="8,0,0,0"></TextBlock>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>-->
        <Border Grid.Column="1">
            <ContentPresenter Content="{Binding DevelopRightViewModel}"/>
        </Border>
    </Grid>
</UserControl>
