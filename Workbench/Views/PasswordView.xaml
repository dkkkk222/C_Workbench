<UserControl x:Class="Workbench.Views.PasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workbench.Views"
             xmlns:controls="clr-namespace:Workbench.Controls;assembly=Workbench.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{DynamicResource TreeViewBackgroundColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Background="Transparent">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="请输入：" FontSize="14" Padding="0,5" Style="{DynamicResource TitleBarStyle}"/>
                        <controls:TitleTextBox Text="{Binding Password, Mode=TwoWay}" TextWidth="205"/>
                    </StackPanel>
                </Grid>

            </Border>
            <Border Grid.Row="1" Background="Transparent" Height="40">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            FlowDirection="RightToLeft"
                            VerticalAlignment="Center">
                    <Button Content="确定" Style="{DynamicResource LeftButtonStyle}" Command="{Binding OkCommand}"/>
                    <Button Content="取消" Style="{DynamicResource RightButtonStyle}" Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
