<UserControl x:Class="Workbench.Views.dw.WatchChartListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workbench.Views.dw"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
            xmlns:calc="clr-namespace:CalcBinding;assembly=CalcBinding"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ItemsControl ItemsSource="{Binding ListCommonDeviceDataEnable,Mode=TwoWay}" IsEnabled="{calc:Binding '_vm.CommonDeviceData.ControlMode==1',Mode=TwoWay}" >
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="4"   Columns="8">
                    </UniformGrid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Grid.Row="0"  Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" >
                        <TextBlock HorizontalAlignment="Center" Text="{Binding DeviceNum}"></TextBlock>
                        <Button BorderThickness="3">
                            <Button.Style>
                                <Style TargetType="Button">                                    
                                    <Style.Triggers>
                                        <DataTrigger Binding="{calc:Binding IsCheck,Mode=TwoWay}" Value="True">
                                            <Setter Property="BorderBrush" Value="Green"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{calc:Binding IsCheck,Mode=TwoWay}" Value="False">
                                            <Setter Property="BorderBrush" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Image Source="/Resources/DeviceW.png" Width="36" Height="36"/>
                        </Button>
                        <TextBlock HorizontalAlignment="Center" Visibility="{calc:Binding 'IsEnable and !FaultStatus and !IsShield',FalseToVisibility=Collapsed}" Foreground="Green" >使能</TextBlock>
                        <TextBlock HorizontalAlignment="Center" Visibility="{calc:Binding '!IsEnable and !FaultStatus and !IsShield',FalseToVisibility=Collapsed}">未使能</TextBlock>
                        <TextBlock HorizontalAlignment="Center" Visibility="{calc:Binding FaultStatus,FalseToVisibility=Collapsed}" Foreground="Red">故障</TextBlock>
                        <TextBlock HorizontalAlignment="Center" Visibility="{calc:Binding 'IsShield and !FaultStatus',FalseToVisibility=Collapsed}" Foreground="Gray">屏蔽</TextBlock>

                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
