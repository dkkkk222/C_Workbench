<UserControl x:Class="Workbench.Controls.Tag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="tag"
             d:DesignHeight="150" d:DesignWidth="400">
    <Grid>
        <Label Content="{Binding ElementName=tag, Path=Text, Mode=TwoWay}">
            <Label.Style>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Label}">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Width="{Binding ElementName=tag, Path=Width, Mode=TwoWay}"
                                        Height="{Binding ElementName=tag, Path=Height, Mode=TwoWay}"
                                        CornerRadius="8"
                                        Padding="10,5">
                                    <TextBlock Text="{TemplateBinding Content}" Foreground="{Binding ElementName=tag, Path=Foreground, Mode=TwoWay}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Label.Style>
        </Label>
    </Grid>
</UserControl>
