<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Common.Controls"
                    >
    <Style x:Key="NoCaptionStyle" TargetType="{x:Type local:NoTitleBarWindow}">
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="WindowChrome.CaptionHeight" Value="0"/>
    </Style>

    <Style x:Key="DefaulClientWindow" TargetType="{x:Type local:NoTitleBarWindow}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NoTitleBarWindow}">
                    <Border Padding="1" CornerRadius="8"
                        Background="#fafafa">
                        <DockPanel LastChildFill="True">
                            <ContentPresenter x:Name="Part_TitleBar" DockPanel.Dock="Top"
                                          IsHitTestVisible="True"
                                          Content="{TemplateBinding TitleBarContent}"
                                          Height="auto"/>
                            <ContentPresenter x:Name="Part_Content" ClipToBounds="True"/>
                        </DockPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="TitleBarContent" Value="{x:Null}"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Part_TitleBar" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="WindChromeCaptionHeight" Value="0"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
